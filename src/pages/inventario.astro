---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.jsx';
import InventoryTable from '../components/InventoryTable.jsx';
// Importa el nuevo componente de React
import InventoryForm from '../components/InventoryForm.jsx'; 
---

<Layout title="Inventario - Gestión de Stock">
  <Navigation currentPath="/inventario" client:load />
  
  <main class="max-w-7xl mx-auto px-4 py-8 bg-dark-700">
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900 mb-4 dark:text-white">
        Inventario Actual
      </h1>
      <p class="text-lg text-gray-600 dark:text-gray-400">
        Consulta y gestiona el stock de todos los talleres
      </p>
    </div>
    
    {/* 🚨 ESTRUCTURA RESPONSIVA CLAVE */}
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      
      {/* Columna 1: Formulario. Ocupa 1/3 en escritorio. */}
      <div class="lg:col-span-1">
        <InventoryForm client:load /> 
      </div>

      {/* Columna 2: Tabla. Ocupa 2/3 en escritorio. */}
      <div class="lg:col-span-2">
        <InventoryTable client:load />
      </div>
    </div>
    {/* 🚨 FIN ESTRUCTURA RESPONSIVA */}

  </main>
</Layout>